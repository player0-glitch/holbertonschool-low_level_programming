#include "main.h"
#include <string.h>

/**
 * _strlen - get the length of string
 * @s: string to get length of
 *
 * Return: returns string's length
 */
int _strlen(char *s)
{
	int i;

	i = (int)strlen(s);
	return (i);
}
