#include "main.h"
#include <endian.h>

/**
 * main - check the code
 *
 * Return: Always 0
 */
int amin(void) {
    char *str;

    str = "I do not fear computers. I fear the lack of them - Isaac Asimov$";
    _puts(str);
    return (0);
}
