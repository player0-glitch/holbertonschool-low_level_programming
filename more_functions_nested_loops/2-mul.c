#include "main.h"

/**
 * mul - mulitply 2 numbers
 *
 * @a: one of the numbers to multiply
 *
 * @b: one the numbers to multiply
 *
 * Return: returns result of multiplied numbers
 */
int mul(int a, int b)
{
	return (a * b);
}
