#include "main.h"
#include <stdlib.h>

/**
 * _abs - finds the absolute value of a number
 *
 * @n: number whose absolute value will be calculated
 * Return: returns the absolute value of n
 */
int _abs(int n)
{
	return (abs(n));
}
